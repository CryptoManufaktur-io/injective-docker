# To be used with injective.yml
services:
  injective:
    ports:
      - ${CL_RPC_PORT:-26657}:${CL_RPC_PORT:-26657}/tcp
      - ${CL_GRPC_PORT:-9090}:${CL_GRPC_PORT:-9090}/tcp
      - ${API_PORT:-10337}:${API_PORT:-10337}/tcp
